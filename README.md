# 🎓 Engineer_GYM - System Practice 트랙

**목표**: “서비스 전체 설계(아키텍처) + 판단 능력” 훈련  
**MVP 루프**: 시나리오 조회 → Mermaid/설명/Tradeoff 제출 → 룰 기반 자동채점(SQL) → 결과 저장/조회

---

## 📁 파일 구조 & 역할

| 파일 | 역할 | 설명 |
|------|------|------|
| **db_01_schema.sql** | 스키마 생성 | DB + 3개 테이블 생성 (scenarios / submissions / results) |
| **db_02_seed_scenarios.sql** | 시나리오 시드 | system_scenarios에 시나리오 Insert |
| **db_03_demo_submission_result.sql** | 데모 실행/채점 | 제출 Insert → 룰 기반 점수 계산 → results 저장 → JOIN 확인 |
| **review_SPOF_bottleneck.py** *(옵션)* | 그래프 분석 | Mermaid를 그래프로 보고 SPOF/병목 탐지(추가 단계) |

> ✅ 이번 회의(또는 1차 완성 범위)는 **SQL(Stage 1~2)** 까지만으로도 충분합니다.  
> (Python 그래프 분석은 “확장 아이디어”로 두셔도 됩니다.)

---

## 🚀 빠른 시작 (추천: MySQL Workbench로 실행)

### Step 0) 가장 흔한 오류 먼저 확인
- Git Bash에서 `mysql: command not found`가 뜨면: **CLI 방식이 아직 불가**한 상태입니다 → 아래 **Workbench 방식**으로 진행하세요.
- Workbench에서 `cp949 codec can't decode...`가 뜨면: 아래 안내대로 **“Open SQL Script로 열어서 편집기에서 실행”**하면 대부분 해결됩니다.

---

## ✅ Step 1) Workbench에서 SQL 실행 (권장)

### 1-1. DB 연결
1) MySQL Workbench 실행  
2) 좌측에서 로컬 커넥션(예: `Local instance MySQL80`) 접속

### 1-2. 파일을 “열어서” 실행하세요 (중요)
> **Server > Run SQL Script**로 바로 실행하면 인코딩 문제(cp949/utf-8)가 날 수 있습니다.  
> 아래 방식으로 하시면 안전합니다.

- 메뉴: **File > Open SQL Script...**
- 파일을 연 뒤, 상단 **번개(Execute)** 버튼으로 실행

### 1-3. 실행 순서
1) **db_01_schema.sql** 실행  
   - DB/테이블 생성
   - 결과로 `SHOW TABLES;`가 3개 테이블을 보여주면 OK

2) **db_02_seed_scenarios.sql** 실행  
   - 시나리오 1개(또는 여러 개) Insert
   - 아래 쿼리로 확인:
   ```sql
   USE Engineer_GYM;
   SELECT id, title, difficulty, created_at
   FROM system_scenarios
   ORDER BY created_at DESC;
